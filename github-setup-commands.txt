# GitHub Setup Commands
# Replace YOUR_USERNAME with your actual GitHub username

# Add GitHub as the remote origin
git remote add origin https://github.com/pratyush456/VehicleBookingApp.git

# Set the main branch (modern GitHub default)
git branch -M main

# Push your code to GitHub
git push -u origin main

# Example with username "pooja":
# git remote add origin https://github.com/pooja/VehicleBookingApp.git
# git branch -M main  
# git push -u origin main